import '../screens/homepage.dart';

class Routes {
  static var routes = {
    '/': (context) => const HomePage(),
    // '/ordertype': (context) => const OrderType(),
    // '/ordercatering': (context) => const OrderCatering(),
    // '/cart': (context) => const Cart(),
  };
}
